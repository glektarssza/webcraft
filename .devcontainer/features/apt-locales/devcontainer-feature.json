{
    "id": "apt-locales",
    "version": "0.0.1",
    "name": "apt-locales",
    "description": "Install the locales package using apt.",
    "keywords": [
        "locales",
        "locale",
        "apt",
        "debian",
        "ubuntu",
        "linux"
    ],
    "options": {
        "generate_locales": {
            "description": "A space-separated list of locales to generate once the package is installed.",
            "type": "string",
            "default": "en_US.UTF-8 en_GB.UTF-8 en_CA.UTF-8"
        },
        "default_locale": {
            "description": "The locale to set as the default locale for the system.",
            "type": "string",
            "proposals": [
                "en_US.UTF-8",
                "en_CA.UTF-8",
                "en_GB.UTF-8"
            ],
            "default": "en_US.UTF-8"
        }
    }
}
